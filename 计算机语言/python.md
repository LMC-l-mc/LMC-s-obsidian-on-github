第一part：print ()记得打印的内容要用“”包着，“”里面的叫字符串。
不知道哪里错了可以把红色的错误信息复制喂给ai，百度
打印“”‘’：看情况外双内单或外单内双或直接在前面放\（转义符）来表明后面就是单纯的单双引号
换行1：\（这个是反斜）后面加的东西自动去到第二行

第二part :用三引号（单或双）可以直接换行
变量：1变量名不能有空格和其它符号，只能由文字，数字和下划线组成，数字不能开头
2变量名不能用数字开头
3不能用引号不然变成字符串
4=左边是变量名（被赋上值）；右边是值

第三part:计算器
要先引入math：import math
第二行：print(math.运算)或先赋值再print
乘：*
n次方：**
除以/（这个是正斜）
和正常数学运算顺序一样：先括号（），再乘方，再乘除，再加减

第四part：#后面那一行是注释不执行

第五part：python里面有不同的数据类型：字符串str，整数int
，浮点数float，布尔类型bool，空值类型none type，列表，字典
程序世界里从0开始数，第一个索引为0，
索引：在字符后面的圆括号里的数字，用来提取在该位置的字符
print(“hello”[2])➡️e
type函数可以返回里面内容的类型
len函数可以得到里面字符串的长度

第六part：命令行模式和交互模式
一般的写完后运行的是命令行模式
交互模式：在python控制台或在window的cmd里先输入python3，然后就可以输入命令，且不需要print就能直接看到输出的结果
输入quit()可以退出交互模式，交互模式的命令都不会被保存

第七part：input
input然后圆括号里面放字符串作为让用户输入的提示信息，并将结果返回为字符串
int能将字符串转化为整数
float能将其他类型转化为浮点数
str可以将其它类型转化为字符串

第八part：条件语句
比较运算符：==
不等于运算符（比较两个值是否不相等）：！=
类似的：<，<=，……
if后面接判断（布尔值），若为true，则执行下方有缩进的命令，默认false的话不执行
如果想要在false时依旧执行，那么在上方缩进命令下方不缩进输入else：，再在下方缩进后输入命令，python会根据缩进判断命令归谁管
格式：if+空格+能输出布尔值（true或false）的运算+冒号
空格+指令
else+冒号
空格+指令
多个条件判断：elif
if+空格+判断1+冒号
空格+指令1
elif+空格+判断2+冒号
空格+指令2
else+冒号
空格+指令3
判断1成立，运行指令1，判断一不成立看判断二是否成立，若成立则执行指令2，不成立则执行指令3

第九part：
逻辑运算符号：and：并且：只有所有的都为true，才会返回true
or：或者，其中一个为true才会返回true
not：不，true变false，false变true
not的优先值最高，其次是and，最后是or，但是可以用括号改变运算顺序

第十part：
列表：在方括号里能放字符串等各种数据
列表名=方括号里放东西
方法：在操作对象后面+.（点）+方法名+括号
函数：函数名+圆括号，圆括号内放入操作对象
append（v.附加）方法：往列表里加字符串，完了之后不需要再重新赋值
remove方法：删去列表里的字符串
用len函数会返回列表里数据的数量
可以通过索引获得某个位置的数据（索引：变量后面直接加方括号，方括号里放数字，从0开始数）
可以用索引赋值改变列表里的数据：a+放括号里放数字=新的数据
列表内置函数max，min，sorted（返回排序好的新列表）

第十一part：字典，能a和b一一对应
 字典（一个数据结构）：用于储存键（key）值（value）对
 键：“”里放东西
 值：可“”里放东西也可直接数字
 字典名={}包住，里面是键和值，键和值用引号包住，键和值之间用引号表示对应，键值对之间用逗号分隔
 获取值：在字典名i后面放方括号，方括号里放用引号包住的键
 元组（tuple）：圆括号里放用引号包住的东西，里面可以放逗号之类的作区分，此时这些元组就是字典里的键
 元组不可变，字典可变，可增删键值对
 添加值或更新值：字典名右边方括号里面放它的键=值
 看键是否存在：打印：键in字典
 删除键值对：del 字典名，方括号里放键
 知道键数：len函数

第十二part：for循环：自动遍历一遍，自动重复执行一段代码
1.for：对列表，字典，字符串等按顺序执行代码
2.格式：for+空格+变量名+in+被操作对象（也是自己取的变量）+冒号：
缩进+操作
3.接列表：变量名会依次被赋予列表里的每一个元素，一直到最后一个
4.接字典：1.方法：字典名.keys加圆括号：返回键
字典名.values加圆括号：返回值
字典名.items加圆括号：返回由键值对组成的元组，要用两个变量名一个接键，一个接值，中间加逗号（自己编，记得左右加空格，中间加逗号即可）。或者一个变量名，然后变量1获取键和变量2获取值（变量1=变量名后方括号里放数字）
for空格+变量+range：range里表示整数数列，圆括号第一个数表示起始值，然后逗号，然后第二个数表示结束值，结束值不在序列的范围内，然后逗号，第三个数表示公差，没有第三个数直接括号时默认公差为1。
5.接字符串：对里面的各个字符按顺序执行代码）
举例：
total=0
for i in range（1，100）（依次取出1，2，3…）
    total=total+i（每取一个数字，就执行一次）
    # 第一次：total=0+1（回忆：左边是变量名，是被赋上值的，右边是值）
    # 第二次：total=1+2
补充：range（）：
单参数：range（x）生成数字：0～x-1
双参数：range（a，b）：a开始，b-1结束
三参数：range（a，b，c）：a开始，间隔是c，b-c结束
搭配for循环：

第十三part：while循环：条件为true，执行里面的代码，执行完回到开头，再次判断条件，直到条件不成立，跳出循环，结束。
如：
i=0
while i<5
   print（i）
   i=i+1
运行：i=0➡️打印0➡️i=1（回忆：左边是变量名，被赋上值，右边是值）➡️（回到开头）打印1➡️i=2……

第十四part：1.format方法
字符串里用花括号表示会被替换的位置，里面的数字表示会用format里面的第几个参数进行替换（还是从0开始数），花括号里的数字后面+冒号+数字+f还可以指定当结果为浮点数时保留几位小数；花括号里还能放变量名，然后在format的参数里放变量名=值，此时变量名里的顺序就无所谓了。
字符串后面+“.”+format+（参数1，参数2，…）
2.f字符串：
在字符串前加f，花括号里加变量名，也可以把值直接运行出来。

第十五part：
sum函数：圆括号里放方括号，放括号里放数字，数字之间用逗号隔开，对数字求和。
type函数：用于获取对象的类型
用def定义函数：告诉python，如果以后有出现函数名，就执行下面的代码。
def后面加空格加自己定义的函数名加圆括号里放参数，参数之间用逗号隔开，然后下一行缩进之后变量名=参数之间的运算
作用域：在函数里面定义的变量都只是局部变量，在函数外面就访问不到了
那么要在函数最后一行缩进后用return+空格+运行结果，一旦运行到return，函数会立刻停止。
python允许一次返回多个数据，它们会被自动打包成一个元组
如：def user（）：
   name=LMC
   age=19
   return name，age
 UserName，UserAge=user（）
 print（User Name）（最后输出LMC）
没用return的话默认返回none
后面想再用的话输入的是函数名而不是被返回的值（返回值表示引用这个函数时输出这个运算后的返回值，后面输入函数输出的是值）
一般的函数像print，.append也不进行变量赋值，一般直接调用
而len，sum这些是带返回值的函数，所以一般会给变量赋值

第十六part：
+=：将左侧变量的当前值与右侧表达式的值相加，然后将结果重新赋值给左侧变量。
同理，-=减法赋值，*=乘法赋值，/=除法赋值（将左侧变量除以右侧的值），//=整除赋值
sorted（【字符串】）（字符串），无key参数时，默认数值从小到大排，大写字母多的字符串优先排在前面，有key参数时，在放括号后面+逗号+等号+len（按长度）或str.lower（忽略大小写顺序）等
模块：一个python程序，引入模块后，里面的函数和变量都能为你所用
%求余数：n%2= =0说明n是偶数，反之= =1则为奇数
引用模块：
方法1:import+空格+模块名
模块名或变量名+.+函数名或变量名
方法2:
from+空格+模块+空格+import+空格+函数名或变量名（多个的话用逗号分隔）
方法3:from+空格+模块名+空格+*（一个星号）

第十七part
过程：是负责完成某个具体任务的代码
对象：把事物先分解到对象身上，描述各个对象的作用，然后才是对象和过程的交互。（面向过程是编年体，面向对象是纪传体）。先定义类，类是对象的模版，对象是类的实例。
面向对象编程的特点：
1.封装：写类的人将内部细节隐藏起来，使用类的人只通过外部接口访问和使用，使用的人不需要知道里面具体是怎么写的。
2.继承：小类拥有大类的属性，减少代码的冗余
3.多态：同样的接口，因为对象具体的类的不同而有不同表现
定义类：
class+空格+类名+：
   def _ _init_ _（前后要有两个下划线）（self，参数1，参数2，…）（括号用来定义对象的属性，可以放任意数量的属性，但第一个参数永远被self占用表示对象自身，帮助把类的属性的值绑定在对象上）：（定义属性）
      self.属性1=（这个类的属性1为…）
      self.…=
    def  方法名1（self，参数1，参数2，…）：（用self的原因是因为可以让我们实现方法调用的结果根据属性的不同而改变）
         函数
     def 方法名2（self，参数1，参数2，…）：
         函数
创建对象：
对象名=类名（“参数1的值”，“参数2的值…”）（给对应属性进行赋值）（字符串之间记得加逗号）
对象名.方法名（具体的方法）
print（对象名.name）➡️属性1的值

函数详解：
_ _init_ _ （initialize）：初始化，当你根据类的“模版”创建一个对象时，这个方法会自动执行，给这个新对象设置初始属性。
self：代表正在被操作的实物，
而后面的则是在创建猫时需要提供的信息，也就是参数，等后面创建对象时再赋值
self.属性：对象的实例属性
方法：调用方法时，如果方法需要输入值就要在方法后面圆括号里放具体的值。
赋值操作：1.创建对象时把具体的值给init里对应的参数，然后参数再把这个值传给对象的实例属性，这样值就会被贴在对象身上了，然后init方法结束，参数会被立刻销毁，但后面调用行为方法时对象的属性就会被贴在上面了。
注意：对象的属性和方法不一定要从参数去获取，也可以在self那里写好。

第十八part：
继承：创建有层次的类
省略创建大类
无特别说明时默认按照父类，如果按照原来的写，则默认优先子类，如果属性和方法有不同，要在父类的基础上增加时：
属性：super（）._ _init_ _ （属性1，属性2）（找父类帮忙初始化）
方法：super（）.方法名
class 子类（父类）：
   def _ _ init_ _ （self，参数1，参数2，…）
      super（）._ _ _init_ _ _（参数1，参数2…）（与父类共有的全部参数）
      self.属性1=参数n（或者是True）
   def   方法名1（self，参数…）
       super（）.父类方法名（）
       函数

第十九part：
open函数：打开目标文件
open(“文件路径”,“模式”，encoding=“编码方式”）（常见的模式包括“r”：读写模式，只读；和“w”：写入模式，只写，这个参数也可以不写，默认为读取模式；现在文件一般的编码方式都是utf-8，编码方式最好写明，否则默认跟随系统）（如果open函数执行成功，会返回一个文件对象，我们可以后续对它进行读取或写入操作）
针对读文件，文件对象有个read方法，readline方法，readlines方法
.read（）：圆括号里可以放数字，表示读多少字节，再用read会继续往下读，圆括号里没有东西没有默认全部读完，返回字符串，没有内容时返回空字符串
.readline（）：只会读取一行的内容，下一次调用就读下一行，它会根据换行符（反斜杠\）来判断一行的结尾，而且换行符也会被当成读到的内容的一部分，一般会和while一起用：
while XX ！=“”：
   操作（和print搭配时注意，readline会把每一行的回车键翻译成换行符\n，然后print又会默认在每行结尾加上换行符，等于换行两次，中间空了一行，而read是全部一起读的不是一行一行，所以没事，解决方法：print（变量名，end=“”）或print（line.strip（）））
   XX=XXX.readline（）（如果不是空字符串，就执行操作，并继续读取下一行）
.readlines（）：会读取全部文件内容，并返回由每行组成的字符串列表（列表名=方括号里放字符串），所以一般会和for循环结合使用，遍历列表里的每一行（注意：如果直接把.readlines打印出来，此时的print会把列表的内部结构也打印出来，也就是说会把换行符\n直接写在字符串里（直接把回车键也打印出来），而在普通的打印字符串时，则会把\n执行为换行操作）（其实是print双标）
读文件名=open（“文件路径”，“r”，encoding=“utd-8”）
变量名1=读文件名.readlines（）
for 变量名2 in 变量名1（不需要定义变量名2，因为变量名2是由变量名1得到的）
   操作
   .close（）（关闭文件）
或者：
with open（…） as 文件名（自己定义的变量）：
   print（读文件名.方法名（））
(这样在缩进的操作执行完毕后文件就会被自动关闭)

第二十part
写文件“w”不需要open的那个文件存在，没有的话它会自己创建，而如果已经存在，反而会把原本的文件清空
with open（“文件路径”，“w”，encoding=“utf-8”） as 变量名（open函数返回的文件对象的名字）：
   变量名.write（“XX”）（针对写文件有write方法）（此时文件里就会出现XX，如果要换行，在句子最后加上换行符\n）
而如果是要在原文后面添加内容的话：
with open（“文件路径”，“a”，encoding=“utf-8”） as 变量名（affiliate（v.n.附属）也是如果不存在则直接创建一个）
同时读写：
with open（“文件路径”，“r+”，encoding=“utf-8”） as 变量名（文字分行：…，\n…，\n）