整个项目的顶层构建脚本，通常不负责具体的编译，而是声明整个项目级别的配置。

- 前缀build:定义文件的职能为构建，即将源代码转化为可执行程序的过程，这个脚本告诉Gradle这个项目要编编译的格式，依赖的库……是Gradle进行施工的“项目蓝图”

- 中缀.gradle：声明文件是专供Gradle引擎读取和解析的

- 后缀.kts:Kotlin Script的缩写，指定使用Kotlin语言。