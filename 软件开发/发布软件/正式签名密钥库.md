Android软件开发中来给发布正式版App签名的加密文件

签名（Signature）：是用私钥对App安装包计算出来的一段加密信息，

- 文件形式：通常是一个.jks或.keystore文件，配合一组别名和密码使用
- 应用商店通过它确认这个App是你发布的，丢失后无法找回，无法更新已上架的App

- 密钥：控制加密，解密，签名，验证等操作的秘密参数，分为对称密钥和非对称密钥
	- 对称密钥：加密